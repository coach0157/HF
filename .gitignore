# --- Dependencies ---
node_modules/
.pnp
.pnp.js

# --- Build output ---
dist/
build/
*.tsbuildinfo

# --- Env / secrets ---
.env
.env.*
!.env.example
!.env.*.example

# --- Logs ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# --- Prisma ---
# Migration history IS committed (it's the schema source of truth applied to
# real databases) — only local/generated artifacts are ignored here.
apps/backend/prisma/dev.db
apps/backend/prisma/dev.db-journal

# --- Testing / coverage ---
coverage/
.nyc_output/

# --- OS / editor ---
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/

# --- Vite ---
apps/admin-web/dist/
apps/admin-web/dist-ssr/
*.local

# --- Docker ---
# (nothing yet — postgres data lives in a named Docker volume, not a bind
# mount, so there is no local data directory to ignore)
