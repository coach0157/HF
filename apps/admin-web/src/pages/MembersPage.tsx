/**
 * Members/houses management — spec 1.3, MVP_BACKLOG.md Epic 5 (P1).
 *
 * Dev agent TODO: CRUD users (resident/guard), assign house_no, list by
 * zone. Depends on the "Users CRUD ระดับพื้นฐาน" TODO in
 * apps/backend/src/modules/auth/auth.module.ts.
 */
export function MembersPage() {
  return (
    <div>
      <h1>จัดการสมาชิก/บ้าน</h1>
      <p>TODO: CRUD users + houses — see component doc comment.</p>
    </div>
  );
}
